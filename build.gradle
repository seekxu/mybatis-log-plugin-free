plugins {
    id 'java'
    id 'org.jetbrains.intellij.platform' version '2.18.1'
}

group = 'com.starxg'
version = '2.0.0'

repositories {
    mavenCentral()
    intellijPlatform {
        defaultRepositories()
    }
}

dependencies {
    testImplementation 'junit:junit:4.13.2'

    intellijPlatform {
        def localIdeaPath = providers.gradleProperty('localIdeaPath')
        if (localIdeaPath.isPresent()) {
            local localIdeaPath.get()
        } else {
            intellijIdea '2024.1'
        }
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

tasks.withType(JavaCompile).configureEach {
    sourceCompatibility = '17'
    targetCompatibility = '17'
    options.encoding = 'UTF-8'
}

intellijPlatform {
    buildSearchableOptions = false

    pluginConfiguration {
        ideaVersion {
            sinceBuild = '241'
            untilBuild = '262.*'
        }

        changeNotes = """
<dl>
    <dt><b>2.0.0</b></dt>
    <dd>Refactored to independent ToolWindow architecture (bottom panel)</dd>
    <dd>Supports IntelliJ IDEA 2024.1 - 2026.2</dd>
</dl>
<dl>
    <dt>1.3.0</dt>
    <dd>support custom colors <a href="https://github.com/starxg/mybatis-log-plugin-free/issues/6">#6</a></dd>
    <dd>fix <a href="https://github.com/starxg/mybatis-log-plugin-free/issues/5">#5</a></dd>
</dl>
<dl>
    <dt>1.2.0</dt>
    <dd>support jump</dd>
    <dd>bug fixes and improvements</dd>
</dl>
<dl>
    <dt>1.1.0</dt>
    <dd>support disable formatting</dd>
</dl>
<dl>
    <dt>1.0.0</dt>
    <dd>Restore mybatis sql log</dd>
</dl>
        """
    }
}